<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
  <%= render @users %>
</ul>
<ol class="microposts">
  <%= render @microposts %>
</ol>

<%= will_paginate %>